# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
# የአይአይ (AI) ቁልፍ (በአገልጋይ ወገን ብቻ የሚቀመጥ)
GEMINI_API_KEY=

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL=

# ENCRYPTION_SECRET_KEY: Hex encoded or base64 32-byte secret key for database level encryption
ENCRYPTION_SECRET_KEY=

# የFirebase ውቅር / Firebase Configuration (በደመና ላይ መረጃ ለማስቀመጥ ከፈለጉ ብቻ ከታች ያሉትን መስመሮች አስተያየት (#) ያንሱ)
# Client-side configuration variables for authenticating with the Firebase project (Uncomment only if you want Cloud database)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=


